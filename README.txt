= hilbert
==== a multi-threaded program to draw hilbert curves

compile with
    make clean && make

usage
    Usage:  ./a1 type order
            type must be A-D.
            order must be 1 through 7.